How to compile and run it??

Compile command:	javac WarehouseContext.java
Run command: 	java WarehouseContext

Once the program it will ask for if you want to retrive all the data that was entered befrore.
If pressed 'y' it will retrive the old stored data.

Then it will show 5 states in the Login State
Then follow the instruction given by the text based UI after this.